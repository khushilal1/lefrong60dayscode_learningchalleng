// function expression
// lwe can declare the function writting on the variable
// lwe can call the function by the variable name
let sum=function(a,b){
    return a+b;
}//sum is the name of functio and 2,3 is the argument of the function which is passed from the calling of function
console.log(sum(2,3));
// the varible used refree das yhe name of funvtin and no anme of function is requred after the (function keyword)
// the function name is not required to be the same as the variable name



sayHi("John"); // Hello, John

function sayHi(name) {
  console.log( `Hello, ${name}` )}

  sayHi(); // Hello, undefined




  